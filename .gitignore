# Rust build artifacts
/target
/dist

# AppImage build dirs
/.appimage-build
/.build-assets

# Runtime secrets & local settings (stored next to the executable)
credentials.json
gdrive-token.json
config.json

# OS / editor cruft
.DS_Store
Thumbs.db
